# Core dependencies for PostgreSQL + OpenAI + MCP Demo
streamlit>=1.28.0
pandas>=2.0.0
openai>=1.0.0
mcp>=0.9.0
fastmcp>=0.1.0
asyncpg>=0.29.0
python-dotenv>=1.0.0

# Optional but recommended
rich>=13.0.0  # For better terminal output in verification script